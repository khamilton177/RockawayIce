
  <!-- <h1>Rockaway Ice Lady Flavors</h1> -->
<div class="container">
 <div class="row">
  <div class="col s4">
  </div>
  <div class="col s4">
    <h3>Water Ices<h3>
    <% @flavor_waters.each do |w| %>
      <%= tag.p("#{w.name}", style: "color: #{@p_colors[@cnt]}") %>
      <% @cnt+=1 %>
    <% end %>

    <h3>Creme Ices<h3>
    <% @flavor_cremes.each do |c| %>
    <%= tag.p("#{c.name}", style: "color: #{@p_colors[@cnt]}") %>
    <% @cnt+=1 %>
    <% end %>
    </div>
    <div class="col s4">
    </div>
  </div>
<div>
